{
  "name": "vulnerability-test-repo",
  "version": "1.0.0",
  "description": "Test repository with known vulnerable dependencies",
  "dependencies": {
    "lodash": "4.17.4",               // Prototype pollution
    "moment": "2.19.1",               // ReDoS vulnerability
    "debug": "2.6.8",                 // Arbitrary file overwrite
    "hoek": "4.2.0",                  // Prototype pollution
    "minimist": "0.0.8",              // Prototype pollution
    "tunnel-agent": "0.4.3",          // Outdated proxy handling
    "stringstream": "0.0.5",          // Remote code execution
    "qs": "6.0.4",                    // Prototype pollution
    "semver": "5.3.0",                // Regex denial of service
    "tar": "2.2.1",                   // Arbitrary file overwrite
    "extend": "3.0.0",                // Prototype pollution
    "deep-extend": "0.4.1",           // Prototype pollution
    "handlebars": "4.0.11",           // Arbitrary code execution
    "marked": "0.3.6",                // XSS vulnerability
    "node-fetch": "1.6.3",            // SSRF vulnerability
    "express": "4.16.0",              // X-Powered-By header info leak
    "jquery": "3.4.0",                // Prototype pollution
    "axios": "0.18.0",                // SSRF and open redirect
    "underscore": "1.7.0"             // Prototype pollution
  },
  "devDependencies": {
    "eslint": "4.18.2",               // Prototype pollution via config
    "webpack": "3.8.1",               // Outdated and insecure plugins
    "babel-core": "6.26.0",           // Outdated dependencies
    "gulp": "3.9.1",                  // Deprecated and insecure deps
    "grunt": "1.0.1",                 // Vulnerable sub-dependencies
    "mocha": "3.5.3",                 // Prototype pollution
    "karma": "1.7.0"                  // Multiple outdated deps
  }
}
